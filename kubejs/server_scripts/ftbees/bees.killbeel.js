/*
onEvent('entity.spawned', event => {
    if (event.server && event.entity.type === 'minecraft:bee') {
        event.server.scheduleInTicks(1, callback => {
            event.entity.kill()
        })
    }
})
 */